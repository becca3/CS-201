#include <iostream>
#include <string>
#include "substring.hpp"

void myStr(std::string str)
{
	for (int i = str.length(); i > 0; i--)
	{
		std::cout << str.substr(0, i) << "\n";
	}
}

void modStr(std::string strMod)
{
	std::string space(" ", str.length());
	for (int i = str.length(); i > 0; i--)
	{

	}
}