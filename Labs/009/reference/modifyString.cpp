#include <iostream>
#include <string>
#include "modifyString.hpp"

int modifyString(std::string& referencedString, int n)
{
	for (int i = 0; i < 3; i++)
	{
		int val = n;
		std::string word;
		std::to_string(n);


		return 0;
	}
}

